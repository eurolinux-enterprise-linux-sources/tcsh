Index: sh.glob.c
===================================================================
RCS file: /p/tcsh/cvsroot/tcsh/sh.glob.c,v
retrieving revision 3.78
retrieving revision 3.80
diff -u -p -r3.78 -r3.80
--- tcsh-6.17.00/sh.glob.c	28 Apr 2010 17:33:19 -0000	3.78
+++ tcsh-6.17.00/sh.glob.c	20 May 2010 22:03:40 -0000	3.80
@@ -781,11 +781,13 @@ backeval(struct blk_buf *bb, struct Strb
 		stderror(ERR_OLD);
 	    alias(&paraml);
 	    t = syntax(paraml.next, &paraml, 0);
+	    if (t == NULL)
+		return;
 	    cleanup_push(t, syntax_cleanup);
 	    /* The F_BACKQ flag must set so the job output is correct if
 	     * printexitvalue is set.  If it's not set, the job output
 	     * will have "Exit N" appended where N is the exit status. */
-	    t->t_dflg = F_BACKQ;
+	    t->t_dflg = F_BACKQ|F_NOFORK;
 	    if (seterr)
 		stderror(ERR_OLD);
 #ifdef SIGTSTP
